// Module partagé : lecture EXIF (parseur binaire custom, sans dépendance).
// À migrer depuis photo2waypoint.
//
// TODO fonctions à fournir :
// - lireCoordonneesGPS(fileBlob) -> { lat, lon } | null si absent
// - lireDateHeure(fileBlob) -> Date | null
